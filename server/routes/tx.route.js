const orderController = require("../controllers/tx.controllers");

const router = require("express").Router();

// router.get("/:uid", orderController.userInfo);
// router.post("/join", orderController.join);
// router.post("/login", orderController.login);
// router.post("/:uid/sendOrder", orderController.sendOrder);

module.exports = router;
